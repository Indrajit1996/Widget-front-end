Steps required to run the app

Step 1: Git clone the app and go to this app folder on terminal

Step 2: "npm install" to install package-lock.json file and node_modules files.

Step 3: "npm run start" to run the app on localhost on port number 8080.
